[project]
name = "trustguard-openenv"
version = "0.1.0"
description = "TrustGuard OpenEnv Environment"
authors = [{ name = "Aswitha Kannoju" }]

dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"
